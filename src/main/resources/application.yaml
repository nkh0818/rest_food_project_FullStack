spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_food_db?serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 
            client-secret:
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

logging:
  level:
    com.yonsai.rest_food_project: info
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace

# 공공데이터 API 키 등 커스텀 설정
road-service-key: ${ROAD_SERVICE_KEY}

# kakao
kakao:
  client-id: ${KAKAO_CLIENT_ID:내_애플리케이션_REST_API_키}
  redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:3000/login/oauth2/code/kakao}
  client-secret: ${KAKAO_CLIENT_SECRET:필요시_설정_내내_발급받은_비밀키}