spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_food_db?serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 
            client-secret:
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

logging:
  level:
    com.yonsai.rest_food_project: info
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: INFO
    org.hibernate.type.descriptor.sql: trace

# 공공데이터 API 키 등 커스텀 설정
road-service-key: 5231251868

# kakao
kakao:
  client-id: 4e546f2d889e9c913a1f9687ef3dba8a
  redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:3000/login/oauth2/code/kakao}
  client-secret: bQKmM8w4v5CYTP05yePxzHDmvaeYuq47
  
kakaoNaviKey: 11389df7116e1a9b5594850fde17f5aa

jwt:
  secret: uFS72wcqyNOfEmbE6f4p6dnT0qiHWMKF

nickname:
  adjectives: 배고픈,바삭한,졸린,질주하는,짭짤한
  nouns: 호두과자,소떡소떡,알감자,델리만쥬,통감자,오징어